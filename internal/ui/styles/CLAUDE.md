# styles

Lipgloss style definitions generated from `theme.Theme`. `New(theme)` returns a `Styles` struct with pre-configured styles for tabs, tables, status bar, dialogs, help overlay, and text (error, warning, success, muted).
